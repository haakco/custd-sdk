self-hosted-runner:
  # Jobs select the shared `haakco-build` label, which the containerd lane serves.
  # The retired DinD selector labels must not come back.
  labels:
    - haakco-build

config-variables: null
