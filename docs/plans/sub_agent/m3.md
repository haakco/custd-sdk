# M3 Mailbox — Exact-Subject v1.6.5 Pre-Push Evidence

**Run ID:** `exact-subject-v165-prepush-r1`
**Identity:** You are the bounded MiniMax-M3 execution worker. Codex owns all
planning, findings, grading, acceptance, Git publication, release mutation, and
archive decisions.

## Task

Execute only Milestone 2 of
`docs/plans/2026-07-18_1948_exact-subject-insight-sdk-parity-plan.md` from the
repository root. Use a thin dispatcher and fresh isolated OpenCode task
subagents for the named observation roles. Run the shared command/state work
serially. Update factual state as required, append one response below, and stop.

Do not start while `.mise.toml` is modified unless Codex has first amended the
owning plan to classify that exact change as in scope. Its current Node-version
delta is unrelated and blocks preflight.

## Indexed Context

Read only the exact paths permitted by each Milestone 2 role. The dispatcher may
also read:

- `AGENTS.md`
- `PROGRESS.md`
- this mailbox
- the owning plan's Current State, Ownership, Milestone 2, Factual Execution
  Ledger, and Primary-Agent Acceptance Checklist sections

Do not preload the whole repository, prior receipts, old mailbox history,
conversation history, implementation reasoning, or reviewer verdicts.

## Writable Paths

- `PROGRESS.md` — factual current/completed/next/blocker/command/file/mailbox
  state only.
- `docs/plans/2026-07-18_1948_exact-subject-insight-sdk-parity-plan.md` — only
  the Factual Execution Ledger rows.
- `docs/plans/sub_agent/m3.md` — append one final factual response below.

Everything else is read-only. Do not create receipts or any other file.

## Constraints

- Run literal commands unchanged. Do not append `;`, `&&`, `||`, pipes,
  redirects, `echo`, exit probes, shell helpers, temporary logs, or wrappers.
- Do not edit source/tests, `BUGS.md`, acceptance cells, findings, or plan prose.
- Do not commit, push, branch, tag, publish, rerun workflows, install packages,
  create/install skills, read credentials, start unrelated MCP servers, or
  mutate external state.
- Do not use `git stash`, reset, checkout-overwrite, force operations, or any
  worktree/history shortcut.
- Ordinary caches created by the prescribed repository gates are allowed.
- One reviewer owns one observation domain. Parallel read-only reviewers may run
  only when they share no mutable state; commands and Git/network state remain
  serial.
- M3 and every M3 reviewer report facts only as `OBSERVED`, `NOT OBSERVED`, or
  `NOT RUN`. Do not author findings, fixes, severity, blocking status, checklist
  marks, `PASS`, readiness, or completion decisions.
- Bounded failure threshold: three correction loops for this non-trivial run.
  Do not retry terminal quota/auth/billing/provider errors until external state
  changes. Confirm the exact task PID and children exit before retry or handoff.

## Acceptance

- Every Milestone 2 preflight, observation, integrated-command, verifier, and
  adversarial row has literal evidence or an explicit `NOT RUN` reason.
- Starting state contains exactly the five named plan-packet deltas and no
  unrelated `.mise.toml` change.
- Starting/ending Git state, changed/generated/temp files, child processes,
  commands, skips, and deviations are fully inventoried.
- The final adversarial role uses OpenCode's native skill tool to load
  `requesting-code-review` and `verification-before-completion`, then OpenCode's
  task tool with `subagent_type: "general"` for one fresh isolated reviewer.
- No unauthorized file, process, command, Git state, credential, or external
  state changes.

## Output

After each material state change, update `PROGRESS.md` and the matching factual
ledger row. Before stopping, append exactly one response under `## M3 Response`
with:

- Status: `READY_FOR_PRIMARY_REVIEW` or `NEEDS_HELP`.
- Run ID and role/subagent IDs.
- Starting and ending Git/process state.
- Exact commands and observed results.
- Every required row as `OBSERVED`, `NOT OBSERVED`, or `NOT RUN` with evidence.
- Changed/generated/temp artifact classification.
- Producer literal-contract self-check and every mismatch/deviation.
- Skipped/unexecuted work and reason.
- Smallest next safe action.

`READY_FOR_PRIMARY_REVIEW` is transport status only, not a verdict. Stop after
the append. Do not commit or push.

## NEEDS_HELP

If any required owner, path, command, expected value, safe action, or permission
is unknown, stop before guessing and append:

- Status: `NEEDS_HELP`
- Unknown
- Evidence checked
- Work completed
- Files/processes changed
- Smallest fact or decision needed

## M3 Response

No response yet.
