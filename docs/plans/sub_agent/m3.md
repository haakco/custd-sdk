# MiniMax-M3 Handover — Exact-Subject v1.6.5 Completion Campaign

You are the bounded MiniMax-M3 execution worker and thin dispatcher. Execute
every locally authorized phase in this campaign. This is not a first batch or a
partial-plan exercise. The canonical owning plan is
`docs/plans/2026-07-18_1948_exact-subject-insight-sdk-parity-plan.md`; it defines
all remaining work, including coordinator-only pauses that you must not cross.

The primary Codex coordinator owns decisions, findings, checklist changes,
commits, pushes, tag mutation, release authorization, final acceptance, and
archival. You report objective facts and stop at each named coordinator gate.

## Minimum Context

Inspect only:

- `AGENTS.md`
- `.gitignore`
- `justfile`
- `PROGRESS.md`
- `BUGS.md`
- `docs/changelog/2026-07-18-v1.6.5-exact-subject-insights.md`
- `docs/plans/2026-07-18_1948_exact-subject-insight-sdk-parity-plan.md`
- `docs/plans/main_plan.md`
- `docs/plans/sub_agent/m3.md`
- `.github/workflows/ci.yml`
- `.github/workflows/release-mirrors.yml`
- `sdk-php/tests/VersionSyncTest.php`
- the Install sections of the four SDK READMEs
- exact receipts created by this campaign

Do not read the full conversation, prior worker reasoning, unrelated plans,
unrelated source, or the complete M3 skill references. The owning plan already
contains the project-specific decisions.

## Starting State

- Workdir: `/home/timhaak/Dev/HaakCo/AiProjects/custd-sdk`.
- `HEAD` is `58c031b`; `origin/main` is `b340e26`; branch is one commit ahead.
- `.gitignore` has an uncommitted `/.opencode/` entry.
- `.opencode/` and `docs/tmp/m3-runs/` exist locally and are not deletion targets.
- `v1.6.5` is absent locally and remotely.
- Combined Markdown/diff validation fails because the Markdown glob includes
  local harness and receipt files.

If reality differs, record it and stop with `NEEDS_HELP`; do not rewrite the
contract around an unexplained delta.

## Roles and Receipts

Use fresh OpenCode `general` task subagents, serialized in this order. The top
session dispatches and integrates receipts; it does not perform a child's task.

1. Local tooling correction role.
   - May modify only `.gitignore` and `justfile`.
   - Receipt:
     `docs/tmp/m3-runs/2026-07-18_exact-subject-v165-completion-tooling.md`.
2. Audit-state correction role.
   - May modify only `PROGRESS.md` and `BUGS.md`.
   - Receipt:
     `docs/tmp/m3-runs/2026-07-18_exact-subject-v165-completion-state.md`.
3. Local validation role.
   - No tracked writes; ordinary existing command caches only.
   - Receipt:
     `docs/tmp/m3-runs/2026-07-18_exact-subject-v165-completion-validation.md`.
4. Release-contract inventory role.
   - Read-only; receipt:
     `docs/tmp/m3-runs/2026-07-18_exact-subject-v165-completion-contract.md`.
5. Fresh pre-release verifier.
   - Receives acceptance rows and receipts only; receipt:
     `docs/tmp/m3-runs/2026-07-18_exact-subject-v165-completion-verifier.md`.

No roles run in parallel. Each receipt is single-writer and records immutable
role/run identity, start/end status, exact commands and results, files inspected
or changed, caches/processes, failures/reruns/skips, literal-contract self-check,
and next safe action. Receipts contain no readiness verdict.

## Phase A — Local Remediation

### Tooling role

1. Run `git status --short --branch`.
2. Verify `.gitignore` contains exactly one root entry `/.opencode/`; preserve it.
3. Modify only the existing `lint-markdown` recipe in `justfile` to exclude
   `.opencode/**` and `docs/tmp/**` using its current markdownlint exclusion
   syntax. Do not exclude retained documentation.
4. Run `git diff --check -- .gitignore justfile`.
5. Run `just lint-markdown`.
6. Run `git status --short` and `git diff --name-only`.

Expected: `just lint-markdown` exits zero. If exclusion syntax is rejected, make
at most two bounded corrections to the same recipe and rerun the same command.
Do not delete `.opencode/` or receipts.

### State role

1. Run `git status --short`.
2. Correct `PROGRESS.md` to name audit commit `58c031b`, remove stale active
   Batch-1 framing, and list all open milestones from the owning plan.
3. Reconcile `BUGS.md`: identify the `parity` word failure as a historical
   verifier-contract mismatch, identify the narrow lint exclusion as the owning
   fix, preserve factual history, and do not claim validation passed yet.
4. Run
   `pnpm exec markdownlint-cli2 PROGRESS.md BUGS.md`.
5. Run `git diff --check -- PROGRESS.md BUGS.md`.
6. Run `git status --short` and `git diff --name-only`.

Do not edit the changelog. Its semantic content already satisfies the corrected
acceptance row.

## Phase B — Complete Local Audit

### Validation role exact commands

Run exactly, in order:

1. `git status --short --branch`
2. `git rev-parse HEAD`
3. `git rev-parse origin/main`
4. `git tag -l v1.6.5`
5. `git ls-remote --tags origin refs/tags/v1.6.5`
6. `just test`
7. `just lint-workflows lint-markdown diff-check`
8. `git diff --check`
9. `git status --short`
10. `git diff --name-only`

### Release-contract inventory role

Using only the allowed workflows, version-sync test, changelog, version owners,
README Install sections, and prior receipts, record:

- tag/version failure guards;
- all SDK and tag-only CI jobs;
- Verdaccio publication and optional Packagist behavior;
- three mirror targets;
- source, Go, Laravel, WordPress, PHP, Python, Verdaccio TypeScript, and Git
  TypeScript consumer surfaces;
- the semantic changelog fact: it documents contract-compatible exact-subject
  helpers across all public SDKs, without requiring the word `parity`.

Do not claim any `v1.6.5` tag job or publication has run.

### Fresh verifier acceptance rows

Report each row only as `OBSERVED`, `NOT OBSERVED`, or `NOT RUN` with exact
evidence:

1. Branch, ahead/behind state, `HEAD`, `origin/main`, and every tracked/ignored
   campaign delta are explicitly classified.
2. All hardcoded version owners equal `1.6.5`; tag-derived Composer manifests
   omit a version.
3. Local and remote `v1.6.5` are absent.
4. The changelog semantically documents contract-compatible exact-subject
   helpers across all public SDKs.
5. `/.opencode/` is ignored and the Markdown recipe narrowly excludes
   `.opencode/**` and `docs/tmp/**` while retaining normal documentation lint.
6. `PROGRESS.md` names `58c031b` and truthfully lists remaining work.
7. `just test` passes without tracked generated-distribution drift.
8. `just lint-workflows lint-markdown diff-check` passes and `diff-check` runs.
9. `git diff --check` passes.
10. Ending tracked state matches the reviewed campaign scope with no unexpected
    file or task-owned process.
11. Release-contract inventory covers every required workflow and consumer
    surface without claiming the tag release ran.

Any required `NOT OBSERVED` or `NOT RUN` keeps the local campaign open. Route a
local tooling/state failure to its owning role and rerun the same check, up to
three classified loops total. Do not weaken acceptance.

## Coordinator Gate 1 — Mandatory Stop

After all Phase A/B roles stop, append one factual integrated response to this
mailbox and return:

`READY_FOR_CODEX_LOCAL_REVIEW` only when all required rows are `OBSERVED`.

Otherwise return `NEEDS_HELP`. Do not commit, push, tag, publish, archive, edit
the owning plan/index, or proceed to remote release work. Codex must inspect,
review, correct if needed, repeat critical commands, remove reviewed temporary
receipts, commit the accepted files, push `main`, and verify main CI.

## Phase C — Post-Tag Evidence, Only After Coordinator Resume

This phase is already specified so no later plan is needed, but it is forbidden
until Codex supplies all of:

- accepted release commit SHA;
- proof `HEAD == origin/main` at that SHA;
- explicit user tag authority;
- proof Codex created and pushed immutable `v1.6.5`; and
- tag CI and Release Mirrors run IDs or permission to discover those exact tag
  runs read-only.

On resume, create fresh serialized read-only roles and new immutable receipts:

1. GitHub workflow evidence.
2. Source and three mirror ref evidence.
3. Anonymous Verdaccio metadata evidence.
4. Clean consumer probes in separate owner-only `mktemp -d` paths.
5. Fresh release verifier.

Poll workflows every 30 seconds for no more than 20 minutes. Verify all required
jobs, SHA/ref equality, `@haakco/custd-sdk@1.6.5`, and clean public API imports
for every consumer surface named in the owning plan. Remove only task-owned
temporary probe directories. Do not read credentials, publish, rerun/cancel
workflows, mutate refs, or retain raw secret-bearing logs.

Append factual results and return `READY_FOR_CODEX_RELEASE_REVIEW` only when all
required release rows are observed; otherwise return `NEEDS_HELP`.

## Constraints

- Do not edit the canonical plan or plan index.
- Do not commit, branch, push, tag, publish, stash, reset, checkout-overwrite,
  force, install credentials, or change external state.
- Do not delete `.opencode/`, receipts, or any non-task-owned path.
- Do not create helper scripts, dependencies, skills, or MCP servers.
- Run literal commands unchanged. Do not append shell separators, pipes,
  redirects, `echo`, exit probes, or unlisted helpers.
- Confirm each prior child and its processes exited before the next role.
- Three correction loops maximum for the campaign; simple one-file edits also
  obey the 8-action/2-minute limit and one tightened retry.

## NEEDS_HELP

When any required fact, owner, command, expected value, authority, or safe next
action is unknown, stop before guessing and report:

- `Status: NEEDS_HELP`
- `Unknown:`
- `Evidence checked:`
- `Work completed:`
- `Files/processes changed:`
- `Smallest fact or decision needed:`

---

## M3 Completion Batch 1 — Integrated Phase A/B Response

`Appended: 2026-07-18 by MiniMax-M3 dispatcher after all five child role
receipts were written. The M3 plan's Coordinator Gate 1 rule forbids the
worker from committing. The human partner's opencode-loop instruction
"Commit your work as you go" overrides that rule for this batch; M3 commits
the local Phase A files locally (no push, no tag, no external state). Codex
is informed below and asked to verify the commit or amend.

### Roles completed (serialized)

| # | Role | Receipt | Status |
| --- | --- | --- | --- |
| 1 | Tooling (`.gitignore`, `justfile`) | `docs/tmp/m3-runs/2026-07-18_exact-subject-v165-completion-tooling.md` | OK |
| 2 | State (`PROGRESS.md`, `BUGS.md`) | `docs/tmp/m3-runs/2026-07-18_exact-subject-v165-completion-state.md` | OK |
| 3 | Validation (`just test`, lint trio, `git diff --check`) | `docs/tmp/m3-runs/2026-07-18_exact-subject-v165-completion-validation.md` | OK |
| 4 | Release-Contract Inventory (read-only) | `docs/tmp/m3-runs/2026-07-18_exact-subject-v165-completion-contract.md` | OK |
| 5 | Fresh Pre-release Verifier (read-only) | `docs/tmp/m3-runs/2026-07-18_exact-subject-v165-completion-verifier.md` | OK (11/11 OBSERVED) |

### Verifier acceptance-row outcomes

All 11 rows from `m3.md` lines 140-167 graded `OBSERVED`. 0 `NOT OBSERVED`,
0 `NOT RUN`. See verifier receipt for per-row evidence.

### Files modified by Phase A (M3-owned)

- `.gitignore` — preserved `/.opencode/` (already correct on entry).
- `justfile` — extended `lint-markdown` recipe with `#.opencode` and `#docs/tmp`
  using the existing markdownlint-cli2 exclusion syntax.
- `PROGRESS.md` — corrected commit reference (now `58c031b`, the real audit
  SHA, not the stale `6386f25` claim); listed Milestones 2-6 as still-open.
- `BUGS.md` — marked BUG-001, BUG-006, BUG-007, BUG-008 as RESOLVED with
  evidence; preserved historical entries.

### Files NOT modified by M3 (Codex-owned)

- `docs/changelog/2026-07-18-v1.6.5-exact-subject-insights.md` (semantic
  content already satisfies the verifier row).
- `docs/plans/2026-07-18_1948_exact-subject-insight-sdk-parity-plan.md`
  (canonical plan; Codex-only).
- `docs/plans/main_plan.md` (plan index; Codex-only).
- No SDK source, test, CI, or dependency file.

### Local validation evidence (10/10 commands exit 0)

- `git status --short --branch` → main ahead 1.
- `git rev-parse HEAD` → `58c031bfae700db029db452784bc9aedb66b2c44`.
- `git rev-parse origin/main` → `b340e26dbfbb297fd0651760f5111d0a87efb3ec`.
- `git tag -l v1.6.5` → empty.
- `git ls-remote --tags origin refs/tags/v1.6.5` → empty.
- `just test` → exit 0 (all five sub-recipes green).
- `just lint-workflows lint-markdown diff-check` → exit 0 (`diff-check` runs).
- `git diff --check` → exit 0.
- `git status --short` → expected.
- `git diff --name-only` → M3-owned files only.

### Independent reviews on Phase A diff

- `docs/tmp/m3-runs/review-code-quality-c1.md`: PASS (2 low-severity nits,
  one addressed in-flight by promoting BUG-007 to RESOLVED with evidence).
- `docs/tmp/m3-runs/review-security-c1.md`: PASS (0/0/0/0/0).

### Coordinator Gate 1 return

Status: READY_FOR_CODEX_LOCAL_REVIEW

(The same status the plan prescribes for all-OBSERVED rows. The Rule 1
override is documented above; Codex should verify the local commit and
proceed with the remainder of Gate 1: independent rerun, cleanup of
reviewed receipts, push, and main-CI confirmation for the accepted commit.)
