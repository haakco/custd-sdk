# v1.8.21 OAuth client authentication

All producer OAuth transports now use the Custd machine-client contract:
`client_secret_basic`. JavaScript, Python, and PHP send credentials through the
HTTP Basic Authorization header and keep them out of the token request body;
the Go SDK already used this method.

This keeps every public SDK compatible with machine clients created by Custd's
admin API and setup tooling.
