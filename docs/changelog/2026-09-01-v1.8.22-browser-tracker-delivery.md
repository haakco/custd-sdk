# v1.8.22 browser tracker delivery

The JavaScript SDK now publishes a self-contained, hashed browser tracker
artifact with a small manifest containing its integrity and size metadata.
Custd can serve one cacheable browser asset without exposing the rest of the
SDK module graph.
