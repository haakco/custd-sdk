# Custd SDK v1.8.5

The hosted browser script now starts its existing SPA tracking after installation, including the initial pageview.
Direct `createBrowserTracker` and `installBrowserTrackerFromScript` callers retain explicit control over tracking.
