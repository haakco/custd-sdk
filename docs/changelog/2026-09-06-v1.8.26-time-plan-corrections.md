# v1.8.26 time-plan correction targeting

Time-plan correction commands in the Go, TypeScript, Python, and PHP SDKs now
target the superseded transition by UUID through the typed
`supersedesTransitionUuid` field. The numeric `supersedesTransitionId` field is
removed so every SDK matches the API contract.
